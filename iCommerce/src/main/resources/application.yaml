server:
  port: 8080
  servlet:
    context-path: /iCommerce



spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/iCommerce"
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true